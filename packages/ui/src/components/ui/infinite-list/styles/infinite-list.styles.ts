import { cva } from 'class-variance-authority'

export const infiniteListEmptyContentStyles = cva('fixed w-full h-full top-0 left-0 -z-40')
export const infiniteListLoaderStyles = cva('flex w-full pt-10 gap-x-2 justify-center items-center text-gray')
